# ============================================================================
# Alation Configuration (Required for MCP Server)
# ============================================================================

# Alation instance URL (no trailing slash)
ALATION_BASE_URL=https://your-company.alation.com

# Alation API access token
# Generate in Alation: User Settings → API Access Tokens → Create Token
ALATION_API_TOKEN=your_api_token_here

# Optional: Alation user ID for user-context operations
# Leave empty if not needed
ALATION_USER_ID=

# ============================================================================
# Slack Configuration (Existing)
# ============================================================================

# Slack Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack App-Level Token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token-here

# Slack Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret-here

# ============================================================================
# AWS Configuration (Existing - for Bedrock)
# ============================================================================

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS Region
AWS_DEFAULT_REGION=us-west-2

# ============================================================================
# PostgreSQL Configuration (Existing - for Vector Store)
# ============================================================================

# PostgreSQL database name
POSTGRES_DB=slack_rag

# PostgreSQL username
POSTGRES_USER=postgres

# PostgreSQL password
POSTGRES_PASSWORD=your-postgres-password

# PostgreSQL host
POSTGRES_HOST=localhost

# PostgreSQL port
POSTGRES_PORT=5432

# ============================================================================
# Usage Instructions
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your actual credentials
#
# 3. Never commit .env to git (it's in .gitignore)
#
# 4. For Alation setup:
#    - Log into Alation instance
#    - Go to User Settings → API Access Tokens
#    - Create new token with read permissions
#    - Copy token to ALATION_API_TOKEN
#
# 5. Test configuration:
#    python app/services/rag/alation_server.py
#
# ============================================================================
